# LED-96 · Login says what the product is — retro (2026-09-25)

## What shipped (`ec25c8b`)
- `src/lib/oauthErrors.ts` (4 tests):
  - `describeOAuthError(code)` maps access_denied, server_error, temporarily_unavailable and a fallback to our own title plus "Try again — nothing was saved".
  - `oauthErrorFromSearch` keeps the old rule of acting only when both `error` and `error_description` are present.
  - The description is never rendered, which replaces the old control-character stripping. Codes are looked up with `Object.hasOwn`, so `constructor` and `__proto__` fall back.
- Two panels at lg: the left-aligned sign-in column with two trust lines ("Delete your data any time" links to /data-deletion), and "What you get" with three capabilities. Below lg the panels stack, and below sm only the capability titles show.

## Acceptance
- Two-panel layout at 1920 that explains the product: PASS. Browser: grid is 960/960, no horizontal overflow at 1920, 768 or 390.
- OAuth error translated, not raw: PASS. `?error=server_error&error_description=server_error: unable to exchange external code` renders "Sign-in didn't complete / We couldn't finish…"; the raw text is absent from the page.

## Backlog
- 11a's install/offline prompt ("Returning here often? Install Ledger").
- 11a's single logo asset: `l-black.png` / `l-white.png` still swap on `theme`.
- In dark mode the right panel's `bg-muted/40` barely separates from the left. Check it in the dark-theme epic (7).
